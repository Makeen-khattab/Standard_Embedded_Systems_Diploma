#include<avr\io.h>
#include"keypad.h"
int main(void)
{

	KEYPAD_init();
	uint8 keyPressed;
	DDRD&=~(1<<4);
	    DDRC|=0x0F;
	    PORTC &= 0xF0;

while(1)
{
	keyPressed=KEYPAD_getPressedKey();

	PORTC=(0xF0 & PORTC) | (0X0F & (keyPressed));


}

}

